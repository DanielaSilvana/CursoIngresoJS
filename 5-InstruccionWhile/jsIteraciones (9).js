/*Enunciado:Al presionar el botón 
pedir números hasta que el usuario quiera,
mostrar el número máximo y el número mínimo.*/


function mostrar()
{
	var contador=0;
	// declarar variables
	
	var pedir;
	var maximo;
	var minimo;
	var numeroIngresado;

	var respuesta='si';
	












	/*while(respuesta!='no')
	{
		contador++
		numeroIngresado=prompt('Ingrese');
		numeroIngresado=parseInt(numeroIngresado);
	}*/ //----------------------------------------//

	if (numeroIngresado<

	//if(contador==1) maximo y minimo son iguales al minimo ingresado?
	//if de maximo y de minimo//
	
	/*numeroIngresado=prompt("")*/

	/*maximo=-99999 positivo o negativo?
	minimo=99999*/

	if (numeroIngresado>maximo)
	{
	
		maximo=numeroIngresado
	
	}

	//if *el mínimo//


/*asdfghjklñ revisame*/


}//FIN DE LA FUNCIÓN